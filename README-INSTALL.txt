Pack updated: tags both SHA and 'latest'. Deploy uses SHA tag; namespace kept 'dev'.

Files to place in repo root:
  Jenkinsfile
  .dockerignore
  helm/flaskapp/.helmignore
  helm/flaskapp/values.yaml
  helm/flaskapp/templates/deployment.yaml
  helm/flaskapp/templates/service.yaml
